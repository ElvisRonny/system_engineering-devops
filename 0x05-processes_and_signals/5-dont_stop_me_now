#!/usr/bin/env bash
# Finds the PID of 4-to_infinity_and_beyond and stops it using kill

# 1. Find the PID of the process
PID=$(pgrep -f "4-to_infinity_and_beyond")

# 2. Kill the process if the PID was found
if [ -n "$PID" ]; then
    kill "$PID"
fi
